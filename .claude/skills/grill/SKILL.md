---
name: grill
description: 以決策樹分輪訪談，窮追猛打地釐清一個計畫、設計或需求，直到沒有任何隱含假設；過程中同步維護專案詞彙表（CONTEXT.md）與架構決策紀錄（ADR）。當使用者想釐清需求、盤問設計、壓力測試自己的想法，或說「grill 我」「幫我釐清」「訪談我」時使用。
argument-hint: "[要釐清的題目]"
license: MIT
metadata:
  adaptedFrom: "https://github.com/mattpocock/skills"
  sourceCommit: "8b36d4fb2635b3c21998dcd8144439c9e5ba7302"
  sourcePaths:
    - skills/productivity/grilling/SKILL.md
    - skills/engineering/domain-modeling/SKILL.md
---

# Grill：需求訪談＋領域建模

窮追猛打地訪談使用者，直到雙方對計畫或設計達成共識。把整個設計視為一棵**決策樹**：每個決策底下掛著依賴它的後續決策。

## 訪談協議

以**輪次**推進決策樹。**frontier** 是「所有前置決策都已敲定、現在就能問而不必猜測未知答案」的問題集合。每一輪把整個 frontier 一次問完：為每題編號並附上你的建議答案，然後等使用者回答完再進下一輪。

每題使用以下格式：

```
❓ **Q1** - **<問題標題>**：<問題內容，可以多段，可含選項>

➡️ <你的建議答案>
```

使用者每輪的回答會重塑決策樹——已敲定的決策把 frontier 往外推，解鎖依賴它們的問題。重新計算 frontier，問下一輪。若某題的答案依賴本輪另一題仍未定的答案，它屬於**之後的輪次**，不是這一輪。

**找事實是你的工作，永遠不是使用者的。** frontier 上的問題若需要環境中的事實（檔案系統、程式碼、工具輸出），派 sub-agent 去查——凡是自己查得到的，不准問使用者。也不要因此卡住：進行中的探索視為未定的前置條件，只有依賴它的下游問題需要等 sub-agent 回報——frontier 上其餘的問題現在就問。**決策**則屬於使用者——逐一提出並等待回答。

frontier 清空即訪談結束：決策樹每個分支都走過，沒有任何事被默默假設。在使用者確認雙方已達成共識之前，不得據此動工。

## 訪談中同步進行領域建模

訪談不只是問答，也要主動打磨專案的領域模型。過程中隨時執行以下紀律：

### 對照詞彙表提出質疑

使用者用了與 `CONTEXT.md` 現有定義衝突的詞時，立即指出：「詞彙表把『取消』定義為 X，但你現在的意思像是 Y——是哪一個？」

### 收斂模糊用語

使用者用了含糊或多義的詞時，提議精確的 canonical term：「你說『帳號』——指的是 Customer 還是 User？這是兩個不同的東西。」

### 用具體情境戳邊界

討論領域關係時，發明會踩到邊界的具體情境來壓力測試，逼出概念之間的精確分界。

### 與程式碼交叉比對

使用者陳述系統行為時，檢查程式碼是否同意。發現矛盾就攤開來：「程式碼是整張訂單取消，但你剛說可以部分取消——哪個才對？」

### 即時更新 CONTEXT.md

術語一敲定，**當場**寫入專案根目錄的 `CONTEXT.md`，不要累積到最後才批次處理。格式見 [CONTEXT-FORMAT.md](./CONTEXT-FORMAT.md)。

`CONTEXT.md` 必須完全不含實作細節。它不是 spec、不是草稿區、不是實作決策的存放處——它是詞彙表，僅此而已。

### 節制地提議 ADR

只有以下三個條件**全部**成立才提議建立 ADR：

1. **難以回頭**——之後改變主意的成本很可觀
2. **沒脈絡會困惑**——未來的讀者看到程式碼會納悶「當初到底為什麼這樣做？」
3. **真實取捨的結果**——確實有其他選項，而你基於特定理由選了這個

缺任何一項就跳過。格式、索引與 supersede 程序見 [ADR-FORMAT.md](./ADR-FORMAT.md)。檔案採 lazy 建立：`CONTEXT.md` 在第一個術語敲定時才建，`docs/adr/` 在第一份 ADR 需要時才建。

ADR 建立後**當場**更新 `docs/adr/README.md` 索引；與既有 `accepted` ADR 衝突時停下來，由使用者決定是修訂目前的想法，還是以新 ADR supersede 舊決策——不要默默推翻。

## 與 Spectra 的銜接

- 訪談達成共識後的結論，可直接作為 `/spectra-propose` 的需求輸入；訪談中確立的檔案路徑與範圍可餵給 proposal 的 Impact 區段。
- 本 skill **絕不**寫應用程式碼、**絕不**建立 Spectra change——那是 `/spectra-propose` 之後的事。訪談結束時，若適合，提示使用者可執行 `/spectra-propose` 進入正式流程。
